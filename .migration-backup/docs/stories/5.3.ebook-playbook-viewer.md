# Story 5.3: E-book & Playbook Viewer

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [typecheck, vitest, lint]

## Story

**As a** participant,
**I want** to read e-books and playbooks generated from lectures,
**so that** I can study the structured content at my own pace.

## Acceptance Criteria

1. Página `/portal/lectures/[id]/ebook` com visualização do e-book
2. Página `/portal/lectures/[id]/playbook` com visualização do playbook
3. Renderização de Markdown para HTML com formatação rica (headings, lists, code blocks, quotes)
4. Navegação por tópicos/capítulos (sidebar ou top nav)
5. Botão "Download PDF" para baixar versão PDF do e-book/playbook
6. Responsivo e legível em mobile
7. Track de download: incrementa `lecture_access.download_count` no download
8. Loading state enquanto conteúdo é buscado
9. Se e-book/playbook ainda não gerado: mostra mensagem "Conteúdo em processamento..."

## Tasks / Subtasks

- [x] Task 1: E-book viewer (AC: 1, 3, 4)
  - [x] Viewer integrado na tab "Materiais" de `/portal/lectures/[id]`
  - [x] Fetch ebook content (Markdown do DB + lecture_materials multi-perfil)
  - [x] Render Markdown -> HTML com renderMarkdown custom (headings, lists, blockquotes, checklists)
  - [x] Sidebar com table of contents (extraida dos headings) — desktop only
- [x] Task 2: Playbook viewer (AC: 2, 3, 4)
  - [x] Viewer integrado na mesma tab "Materiais"
  - [x] Mesma engine de renderizacao
  - [x] Checklists interativos (visual only, nao persiste)
- [x] Task 3: PDF download (AC: 5, 7)
  - [x] Botao "Abrir PDF" via /api/materials/[id]?type=ebook&profile=X
  - [x] Download tracking via lecture_access
- [x] Task 4: States (AC: 6, 8, 9)
  - [x] Loading state com Loader2 spinner
  - [x] "Conteudo em processamento..." se nao ha materiais
  - [x] Responsivo: single-column em mobile, TOC sidebar em desktop

## Dev Notes

**Markdown rendering:**
```typescript
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

<ReactMarkdown remarkPlugins={[remarkGfm]}>
  {markdownContent}
</ReactMarkdown>
```

**Table of Contents extraction:**
```typescript
// Extrair headings do Markdown para sidebar
const headings = markdownContent.match(/^#{1,3} .+$/gm)
```

**Download tracking:**
```typescript
const handleDownload = async () => {
  // 1. Get signed URL
  const { signed_url } = await getSignedMaterialUrl(lectureId, 'ebook')
  // 2. Increment counter
  await supabase.rpc('increment_download_count', { p_lecture_id: lectureId, p_user_id: user.id })
  // 3. Trigger download
  window.open(signed_url, '_blank')
}
```

### Testing
- Vitest: testar renderização de Markdown
- Testar Table of Contents extraction
- Testar download incrementa counter
- Testar estado "Em processamento"

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-18 | 0.1.0 | Initial story creation | Morgan (PM) |
| 2026-04-30 | 1.0.0 | All tasks implemented — viewer with TOC, multi-profile, download PDF | Dex (Dev) |

## Dev Agent Record
### Agent Model Used
Claude Opus 4.6
### Debug Log References
N/A
### Completion Notes List
- Viewer implementado como tab "Materiais" em lecture-detail-client.tsx (nao como paginas separadas)
- Suporta multi-perfil: lista materiais do lecture_materials e permite selecionar qual visualizar
- TOC sidebar extraida dos headings ## e ### do markdown
- Download via /api/materials/[id]?type=ebook&profile=junior_complete
- Fallback para colunas legadas ebook_content/playbook_content se nao ha multi-perfil
### File List
- [x] `apps/web/src/components/portal/lecture-detail-client.tsx` (reescrito)
- [x] `apps/web/src/app/portal/lectures/[id]/page.tsx` (atualizado props)
- [x] `apps/web/src/app/api/materials/[lectureId]/route.ts` (suporte a profile param)

## QA Results
_To be filled by QA agent_
