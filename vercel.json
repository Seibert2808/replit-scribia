{
  "buildCommand": "pnpm --filter @workspace/scribia build",
  "outputDirectory": "artifacts/scribia/dist/public",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
